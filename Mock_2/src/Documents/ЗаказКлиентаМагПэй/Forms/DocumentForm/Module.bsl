
&НаКлиенте
Процедура ОбработатьЗаказ(Команда)
	ОбработатьЗаказНаСервере();
КонецПроцедуры

&НаСервере
Процедура ОбработатьЗаказНаСервере()
	
	Объект.state = Перечисления.СостоянияЗаказовМагПэй.SOLD;
	Объект.type = Перечисления.ТипыОперацийФискальногоРегистратораМагПэй.SELL;
	
	ПараметрыЗаписи = Новый Структура;
	ПараметрыЗаписи.Вставить("РежимЗаписи", РежимЗаписиДокумента.Проведение);
	Записать(ПараметрыЗаписи);
	
КонецПроцедуры
